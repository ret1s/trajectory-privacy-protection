"""Implementation package for the dummy-generation benchmark harness.

The stable command-line and import surface remains
``experiments.run_dummy_benchmark``.  This package keeps the implementation
split by responsibility so the compatibility module stays small.
"""
